# intake - контекст AI-агентов

Канонический файл проекта. Держим коротким; детали живут в `docs/`.

Общие конвенции проектов (язык, ветки, релизы, CHANGELOG,
безопасность) живут во внутренней базе знаний; здесь только специфика проекта.

Репозиторий: `github.com/daniil4545/tg-intake`, публичный. Всё, что попадает в
коммит, читается посторонними: ни секретов, ни персональных данных, ни
внутренних имён сотрудников и клиентов.

## Проект

Сервис `intake` (репозиторий `tg-intake`, продуктовое имя «Предложка») -
внутренний Telegram-бот, единая точка входа бизнеса в разработку. Бот принимает
обращения сотрудников по работающим сервисам, доводит их интервью до пригодного
к работе описания, заводит тикет в GitHub Issues нужного проекта, показывает его
статус по меткам и даёт автору отменить свой тикет; проект подключается командой
`/project`. Следом идут брифинг нового проекта и вопросы по документации.
Границы, порядок функций и деление - [docs/prd.md](docs/prd.md).

Цель - убрать владельца из роли переводчика между сырым фидбеком и агентом.
Это внутренний инструмент: минимальная рабочая модель важнее продуктового polish.

## Принципы

1. **Core против Изменяемого.** Каркас (поток, схема БД, контракты интеграций,
   инварианты) меняется дорого и защищается. Правила поведения (контракт
   готовности, тексты, предел вопросов) живут данными и меняются свободно.
   Разделение по функциям - раздел 4 PRD. Правка правил не должна требовать
   миграции.
2. Минимум кода: ни функций, ни интерфейсов, ни файлов «на будущее». Структура
   плоская, имя файла равно ответственности, комментарий объясняет только
   неочевидное «почему».
3. Сначала модель состояния, потом код.
4. Побочные эффекты выполняет детерминированный Go. LLM возвращает недоверенный
   структурированный вывод; Go валидирует его до БД, outbox и внешних API.
5. Логирование - часть каждого среза: действие автора и решение сервиса
   наблюдаемы, новый side effect сопровождается логом.
6. Не коммитить секреты, `.env`, токены.

## Стек

Модуль `github.com/daniil4545/tg-intake`. Прямых зависимостей три, остальное -
стандартная библиотека; обоснование - раздел 1 [architecture](docs/architecture.md).

- Go 1.25, PostgreSQL 16 (данные и очередь через `FOR UPDATE SKIP LOCKED`)
- `gopkg.in/telebot.v4` - Bot API, кнопки, long polling
- `github.com/jackc/pgx/v5` - драйвер и пул
- `github.com/pressly/goose/v3` - миграции, бинарь в образе
- `log/slog` (stdlib) - JSON-логи; OpenRouter и GitHub - свои клиенты на
  `net/http`, SDK не тянем; ffmpeg не нужен - модель принимает ogg/opus
- Модели: `OPENROUTER_MODEL_MEDIA` (голос и скриншоты,
  `google/gemini-3.1-flash-lite`), `OPENROUTER_MODEL_DIALOG` (интервью и
  саммари, `deepseek/deepseek-v4-flash-0731`)

## Структура

```text
intake/
├── docs/     # prd, architecture, contracts, state, backlog, plans/, acceptance/
├── deploy/   # safe-ssh.sh и prod/docker-compose.coolify.yml
└── src/      # cmd/intake/, migrations/, Makefile, Dockerfile, .env.example
    └── internal/app/  # весь рабочий код плоско, плюс prompts/ и rules/
```

## Команды

Всё из `src`: `docker compose up -d postgres`, `make migrate-up`, `make run`,
`make test`, `make vet`, `make ci-check`, `make migrate-reset`.
`migrate-*` отказываются работать с не-localhost базой без `ALLOW_REMOTE=1`.

Обязательные переменные: `DATABASE_URL`, `TELEGRAM_BOT_TOKEN`,
`TELEGRAM_ALLOWED_IDS`, `OPENROUTER_API_KEY`, `GITHUB_TOKEN`; проекты контура -
`PROJECTS`, уведомления владельцу - `ALERT_CHAT_ID` и `ALERT_BOT_TOKEN`. Полный
список с дефолтами - `src/.env.example`.

Диагностика контура - только через `deploy/safe-ssh.sh` из корня; без
аргументов печатает список из 12 действий. Все они read-only, произвольный ssh
к серверу не используем. `cases` и `case` печатают тексты обращений и ФИО
автора: решением владельца это допустимо в терминале, но не в коммите.

Проверка выката: зелёный workflow означает «Coolify принял запрос», а не «версия
выкачена» - сверять `app_revision` из `health` с кандидатом, старый контейнер
остаётся healthy и при несостоявшемся деплое. Заведённые в контуре переменные
показывает `deploy-config`; `app-env` - лишь окружение текущего контейнера.

## Workflow

Пайплайн задачи и инженерные дефолты платформы - внутренняя база знаний вне
репозитория. Технические решения - `docs/architecture.md`; нетривиальный срез
получает спеку `docs/plans/plan-*.md` до реализации.

Ветки: единственная - `prod`; срез в `feature/<slug>`, PR в `prod`, после
мерджа ветка удаляется. Деплой контура только по тегу `v*` и команде владельца
(workflow `Deploy`); автодеплоя по push нет. Отдельного dev-контура нет:
понадобится - заводится заново по runbook базы знаний.

Метод интервью взят из промта брифовщика (внутренняя база знаний): разбор
конкретного случая, детектор выдумки, замок пересказа «где я ошибся», признак
тянет вопрос. Промт бота - самостоятельный файл, а не копия: другой жанр и
бюджет вопросов; при правках сверяться с оригиналом, не тащить его целиком.

## Критические инварианты

- **Медиа не переживает обращение.** Файл нужен до подтверждения саммари
  автором; после публикации тикета файлы удаляются, в БД и в issue остаётся
  текст и `file_id` Telegram. Изображение не попадает ни в git, ни в логи.
- **Обезличивание до публикации.** Экстрактор возвращает описание без
  персональных данных клиентов: идентификаторы карточек и статусы остаются,
  ФИО, телефоны и переписка - нет. Транскрипт чистится теми же правилами.
- **Единственный источник истины по статусу - метка issue.** Свои метки сервис
  ставит дважды: `type:`, `status:new`, `author:` и при недоборе `incomplete`
  при публикации, `status:cancelled` при отмене. Дальше он их только читает: кто
  сменил метку, человек или CI, сервису неизвестно и неважно.
- **Экстрактор обязан признаваться в непрочитанном.** Схема извлечения содержит
  поле «не разобрано». Правдоподобная выдумка хуже явного пробела: её примут
  за факт.
- **Тикет не публикуется без подтверждения автора.** Саммари с вопросом «где я
  ошибся» - последняя точка, где ловится неверно прочитанный скриншот, пока
  файл ещё существует.
- **Нормализация до диалога, изображение последним.** Аудио и текст
  нормализуются первыми, скриншоты разбираются после них и с протоколом сырья в
  контексте. Медиа входит в модель один раз, в историю диалога - только текст.
- **Авторство не теряется.** ФИО из Telegram уходит в шапку тела issue и в метку
  `author:<slug>`: GitHub не даёт создать тикет от чужого имени, поэтому автор
  фиксируется телом, а не полем API. Обезличивают данные клиентов, не автора.
- **Доступ по белому списку Telegram ID.** Список в конфиге; чужому бот отвечает
  отказом и обращение не заводит.
- **Егресс наружу может быть закрыт.** С хоста контура прямой путь к OpenRouter
  отвечает `403` от фильтра по дороге. Прокси задаётся каждому клиенту отдельно
  (`OPENROUTER_PROXY`, `TELEGRAM_PROXY`), пустое значение - прямой путь: общий
  прокси процесса увёл бы в туннель и GitHub, доступный напрямую.
- **Порядок промта фиксирован:** стабильный префикс (инструкция плюс контекст
  проекта) впереди и кэшируется, волатильное (протокол сырья, история) сзади.
  Ни одной изменяющейся строки в префиксе, иначе кэш молча перестаёт работать.
- **Ни одна идея не выбрасывается.** Отклонение всегда с причиной и доступно
  автору в боте. Недобранный контракт даёт тикет с меткой `incomplete` и явным
  списком пробелов, а не отказ.
- **Мультипроектность с первого дня.** Проект - строка в таблице (`owner/repo`
  и контекст для промта). Токен GitHub один на сервис, в таблице ключей нет.
  Команда `/project` заводит проект без деплоя; список `PROJECTS` из конфига
  контура синхронизируется в ту же таблицу при старте.
- **Работа с побочным эффектом идёт через таблицу `jobs`**, идемпотентна по полю
  `key` и оставляет аудит в `case_events`. Синхронно из обработчика апдейта
  уходят ответы автору, скачивание вложения и чтение GitHub для `/tickets` и
  `/project`: читающему вызову очередь не нужна, автор ждёт ответа сразу.
- **Нормализация не знает, чем кончится разговор.** Сырьё превращается в текст
  одинаково для любого режима: в шаге нормализации нет ни типа тикета, ни
  issue. Это то, что делает вторую функцию бота наслаиванием, а не переписью.
