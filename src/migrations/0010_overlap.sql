-- +goose Up
-- Пересечения черновика с состоянием проекта: готовый список пунктов со
-- ссылками. Хранится markdown-ом, потому что уходит в два места - карточку
-- саммари и тело issue, - а собирается один раз на шаге саммари.
--
-- Пусто значит «сверка ничего не нашла» и «сверка не состоялась» разом: шаг
-- необязательный, и отличать эти случаи в потоке автора нечем.
ALTER TABLE cases ADD COLUMN overlap text NOT NULL DEFAULT '';

-- +goose Down
ALTER TABLE cases DROP COLUMN overlap;
