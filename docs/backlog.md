# Backlog

Границы и деление функций - [prd.md](prd.md), технические решения -
[architecture.md](architecture.md). Здесь только порядок работ.
Runtime-снимок - [state.md](state.md).

## Milestones

- [x] **M0** Каркас: Go, PostgreSQL, миграции, конфиг, белый список, JSON-логи,
      меню проектов, Docker, CI. Код готов, приёмка ждёт владельца.
- [ ] **M1** Приём и нормализация: меню проекта и действий, сбор пачки до
      «Готово», транскрибация голосового, склейка протокола сырья, извлечение из
      скриншота с протоколом в контексте, удаление файлов.
- [ ] **M2** Интервью и публикация: контракт готовности, добивание раундами по
      2-3 вопроса, саммари с подтверждением, создание issue через outbox с
      авторством в шапке и метке, ответ автору. Замыкает Core.
- [ ] **M3** Dev-контур: Coolify, база, автодеплой из `dev`, живой прогон.
- [ ] **M4** Просмотр и жизненный цикл: список тикетов, карточка, статусы через
      метки, уведомления, комментарии, отмена, отклонение с причиной.
- [ ] **M5** Защита от мусора: дубли, лимиты. Нужен при открытии доступа шире
      белого списка.

M0-M3 составляют Core и дают работающий сервис. Версия к 2026-08-10 - это
M0-M3, только создание тикета. M4 делает из сервиса трекер и в срок не входит.

## Активные задачи

- [ ] **Блокеры от владельца**: список Telegram ID для белого списка,
      fine-grained PAT GitHub с правом Issues read/write на репозитории
      проектов. Токен бота и dev-контур Coolify получены.
- [ ] **Устойчивость к морганию Telegram**: один неудачный `getMe` на старте
      роняет процесс, спасает только `restart: unless-stopped`. Егресс к
      Telegram с этого VPS нестабилен даже с пином DC. Нужен повтор с
      отсрочкой при инициализации бота.
- [ ] **Проверить живьём приём ogg/opus** моделью Gemini через OpenRouter:
      модели с аудио на входе есть, поддержка контейнера ogg заявлена как
      зависящая от провайдера. Не принимает - ставим конвертацию ffmpeg в mp3
      безусловно.
- [ ] **Golden-набор из реальных обращений сотрудников**: подать боту исходные
      голосовые и скриншоты, сравнить полученный тикет с тем, что в итоге
      написано руками в спеке. Единственный способ проверить качество интервью.

## Отложено осознанно

- Веб-интерфейс, дашборды, отчёты.
- Приоритеты, сроки, назначение исполнителей.
- Редактирование тела созданного тикета.
- Поиск и фильтры в списке тикетов.
- Автоматическая простановка меток статуса из CI: начинаем с ручной, сервис
  об этом не знает.
