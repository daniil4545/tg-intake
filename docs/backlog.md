# Backlog

Границы и деление функций - [prd.md](prd.md), технические решения -
[architecture.md](architecture.md). Здесь только порядок работ.
Runtime-снимок - [state.md](state.md).

## Этап 1 - MVP, закрыт релизом 0.1.0 (2026-08-09)

- [x] **M0** Каркас: Go, PostgreSQL, миграции, белый список, CI, контур Coolify.
- [x] **M1** Приём и нормализация: пачка до «Готово», транскрибация, протокол
      сырья, разбор скриншотов, удаление файлов.
- [x] **M2** Интервью и публикация: контракт готовности данными, раунды
      вопросов, саммари с подтверждением, issue через outbox.
- [x] **M3** Контур: Coolify, база, деплой из CI.
- [x] **M4** Просмотр тикетов и проекты: `/tickets`, карточка, статусы метками,
      отмена автором, `/project`. Кнопочное управление: закрытый вход, панель
      «Меню | Сброс», глобальный «Сброс». Принят владельцем 2026-08-09 решением
      о закрытии MVP; живые прогоны - issues 28 и 29 в `qualifier`.

## Этап 2 - от приёма обращений к сопровождению проектов

- [ ] **M6** Брифинг: режим «завести новый проект», интервью по промту
      брифовщика, на выходе PRD документом в чат. Спека после первого живого
      прогона промта с заказчиком вне бота; на ней решаются хранение PRD,
      адресация сообщений при двух активных разговорах и правка
      тикет-центричных текстов.
- [ ] **M7** Вопросы по проекту: ответ из накопленной документации. Условие
      входа - PRD проектов и история тикетов в системе.
- [ ] **M8** Уведомления о статусе: фоновый опрос меток issue, сообщение автору
      о смене. Условие входа - работающий просмотр показал, каких новостей
      автору не хватает.
- [ ] **M9** Комментарии к тикету: дополнение сырьём. Условие входа - решение
      о двух активных разговорах, то же, что принимается на M6.
- [ ] **M5** Защита от мусора: дубли, лимиты. Нужен при открытии доступа шире
      белого списка.

Порядок: M6 первым - его срочность снята лишь тем, что промт брифовщика
работает в чате Claude уже сейчас. Сроки плавающие по решению владельца
2026-08-08: за датами нет внешнего события.

## Активные задачи

- [ ] **Расширять `GITHUB_TOKEN` при заведении проекта**: fine-grained PAT
      выдаётся на конкретные репозитории. Без права `/project` честно откажет.
      planerka и qualifier токеном уже покрыты (владелец, 2026-08-09).
- [ ] **Живость поллера в healthcheck**: telebot не отдаёт ошибки опроса,
      healthcheck проверяет базу; локальный поллер с тем же токеном молча
      забирает апдейты (`409 Conflict`), контур остаётся healthy.
- [ ] **Один токен - один поллер**: локальный запуск обязан отличаться токеном
      от контура; отдельный тестовый бот в `.env.example`. Решать вместе с
      живостью поллера.
- [ ] **Уведомление, исчерпавшее повторы, молчит навсегда**: у `HandleFailedJob`
      нет ветки `notify`, недоступность Telegram дольше минуты теряет вопрос
      раунда или номер тикета без следа для автора (ревью 2026-08-09).
- [ ] **`replaceJob` держится на конвенции**: DELETE без фильтра статуса может
      снять уже захваченную работу; корректность обеспечивают версионные
      проверки вызывающих. Сюда же гонка двойной отмены тикета: чинится
      транзакцией либо уникальным индексом `case_events(case_id, kind)`.
- [ ] **Скриншоты одного обращения разбираются одним job**: при деградации
      OpenRouter пачка не влезает в 4-минутный бюджет и уходит в повторы.
      Разбивать по элементу, как голосовые (ревью 2026-08-09). С 0.1.3 острее:
      попытка длится до двух минут, и один медленный скриншот оставляет
      следующие без бюджета; разобранное при этом остаётся `done`, повтор
      работы продолжает с оставшихся (ревью 2026-08-12).
- [ ] **Мелочи UX из прогона сценариев 2026-08-09**: окно 10 секунд после меню
      глотает одиночное осмысленное сообщение; «Продолжить» из меню другого
      проекта молча перенацеливает обращение; двойное «Всё так» отвечает
      текстом про прошлый вопрос; карточка при недоступном GitHub показывает
      «Отменить тикет» и для закрытого; правка сообщения автором игнорируется.
- [ ] **Порог готовности зависит от формы обращения**: аккуратный текст
      проходит без вопросов, голосовое даёт раунд. Смотреть на обращениях
      заказчика; бедный тикет - поднимать планку промта.
- [ ] **Диалог перегружен сообщениями**: кандидаты на сжатие - сбор сразу после
      выбора проекта, протокол вместе с первым раундом. Решение владельца
      2026-08-08: смотреть на практике, пока не трогаем.
- [ ] **Golden-набор из реальных обращений**: подать боту исходное сырьё,
      сравнить тикет с написанным руками. Единственная проверка качества
      интервью и арбитр выбора моделей.
- [ ] **Один источник истины по формату ТЗ**: `plan-briefing.md` и шаблон PRD
      брифовщика описывают один артефакт по-разному. Свести до M6, вне этого
      репозитория.
- [ ] **Имя compose-проекта**: `name: intake` из-за совпадения каталога `src` у
      соседей; локальный 5432 занят - тестовый Postgres на своём порту.
- [ ] **Уборка очереди при следующей миграции `jobs`**: `'remind'` в CHECK не
      используется, таблица jobs не чистится, сканы по `payload->>'case_id'`
      без индекса. Заметно на порядки позже текущего масштаба.
- [ ] **Удалить механику «вопрос проекта внутри сбора»**: `askProject` из
      `CollectItem` стал защитой от старых данных; убрать вместе с
      `ErrNoProject`, когда живые прогоны подтвердят новый поток. Там же
      ловушка пустого списка проектов при активном обращении.

## Отложено осознанно

- Отдельный dev-контур: с 0.1.0 единственный контур считается прод; понадобится
  песочница - заводится заново по runbook.
- Веб-интерфейс, дашборды, отчёты.
- Приоритеты, сроки, назначение исполнителей.
- Редактирование тела созданного тикета; поиск и фильтры в списке.
- Выключение проекта из бота (только через базу).
- Автоматическая простановка меток статуса из CI: начинаем с ручной.
