# Backlog

Границы и деление функций - [prd.md](prd.md), технические решения -
[architecture.md](architecture.md). Здесь только порядок работ.
Runtime-снимок - [state.md](state.md).

## Milestones

- [x] **M0** Каркас: Go, PostgreSQL, миграции, конфиг, белый список, JSON-логи,
      меню проектов, Docker, CI, dev-контур. Принят 2026-08-08.
- [ ] **M1** Приём и нормализация: меню проекта и действий, сбор пачки до
      «Готово», транскрибация голосового, склейка протокола сырья, извлечение из
      скриншота с протоколом в контексте, удаление файлов.
- [ ] **M1.5** Ревизия под конечную рамку: сверить код M1 с тремя дисциплинами
      наслаивания, записать места схемы, заточенные под один режим, дописать
      спеку M2. Кода почти не меняет, границы правки - ниже.
- [ ] **M2** Интервью и публикация: контракт готовности, добивание раундами по
      2-3 вопроса, саммари с подтверждением, создание issue через outbox с
      авторством в шапке и метке, ответ автору. Замыкает Core.
- [x] **M3** Dev-контур: Coolify, база, автодеплой из `dev`, живой прогон.
      Сделан вместе с M0: без контура срез нечем принимать.
- [ ] **M4** Просмотр тикетов: список по проекту, карточка с телом, статусы
      через метки, отмена автором, причина отклонения в карточке. Уведомления и
      комментарии вынесены из среза решением 2026-08-08.
- [ ] **M5** Защита от мусора: дубли, лимиты. Нужен при открытии доступа шире
      белого списка.
- [ ] **M6** Брифинг: режим «завести новый проект», интервью по промту
      брифовщика, на выходе PRD документом в чат. Спека пишется после первого
      живого прогона промта с заказчиком вне бота. Три решения принимаются на
      ней: где хранится собранный PRD (иначе M7 остаётся без корпуса), как
      автор адресует сообщение одному из двух активных разговоров, и правка
      тикет-центричных текстов бота под пришедшего заказчика.
- [ ] **M7** Вопросы по проекту: ответ из накопленной документации. Условие
      входа - PRD проектов и история тикетов в системе.
- [ ] **M8** Уведомления о статусе: фоновый опрос меток issue, сообщение автору
      о смене. Вынесен из M4: три рабочих статуса на десяток тикетов дают до
      тридцати сообщений за цикл, и статус нужен тогда, когда о нём вспомнили.
      Условие входа - работающий просмотр показал, каких новостей автору не
      хватает.
- [ ] **M9** Комментарии к тикету: дополнение сырьём с голосом и скриншотами.
      Вынесен из M4. Условие входа - решение о двух активных разговорах у
      автора, то же самое, которое принимается на M6.

M0-M3 составляют Core и дают работающий сервис. Первая версия - только создание
тикета; M4 делает из сервиса трекер и в неё не входит. Срок плавающий по
решению владельца 2026-08-08: оценка показала объём двух полных срезов, а за
датой нет внешнего события.

Порядок после M2: сначала M4, потом M6. M4 замыкает контур первой функции - без
статусов автор возвращается спрашивать в личку, и тикеты работают наполовину.
M6 добавляет новую функцию, и его срочность снята тем, что промт брифовщика
работает в чате Claude уже сейчас, без бота.

### Границы M1.5

Правим только то, что делает второй режим невозможным без переписывания. Что
было бы красивее - не трогаем: M0 и M1 закрыты.

- [x] Документы под конечную рамку: PRD разделы 1-2 и 10, backlog, state.
- [x] Сверить сборку промта: стабильный промт из `embed` идёт системным
      сообщением, протокол и изображение - пользовательскими следом. Порядок
      верный, волатильного в префиксе нет.
- [x] Прочитать `case.go` по четырём адресам ревью. Тикет-специфики в потоке
      нет. Осталось два адреса на будущее, записаны в раздел 13 архитектуры.
- [x] Записать в архитектуру места, заточенные под один режим, с ценой
      изменения. Схема не менялась.
- [x] Спека M2 написана: [plan-m2-interview.md](plans/plan-m2-interview.md).
      Контракт готовности вынесен в `rules/contract.yaml`, предел раундов в
      конфиг, история интервью строится из `case_events` без новых таблиц.

Проверено по коду и повторять не нужно: `normalize.go` не знает про тикет,
`worker.go` держит обработчики мапой по типу работы, в потоке нет ни типа
обращения, ни issue.

## Активные задачи

- [x] **Выдать `OPENROUTER_API_KEY` в dev-контур**: заведён владельцем
      2026-08-08, живой прогон разблокирован.
- [ ] **Имя compose-проекта**: `name: intake` задано, потому что каталог `src`
      совпадает у соседних сервисов и команды одного проекта управляли
      контейнерами другого. Локальная база остаётся занятой соседом - проверять
      на своём порту.
- [x] **Прокинуть `GITHUB_TOKEN` в Compose**: заведён в dev-контуре, доходит до
      контейнера, проверяется при старте по каждому активному проекту.
- [ ] **Устойчивость к морганию Telegram**: неудачный `getMe` на старте роняет
      процесс, спасает только `restart: unless-stopped`. Повтор с отсрочкой, M1.
- [ ] **Проверить живьём приём ogg/opus** моделью Gemini: поддержка контейнера
      заявлена как зависящая от провайдера. Не принимает - ffmpeg в mp3
      безусловно. Первый шаг M1.
- [ ] **Живость поллера в healthcheck**: дешёвого признака нет, telebot не
      отдаёт ни пустые циклы опроса, ни его ошибки, а healthcheck проверяет базу.
      Подорожало 2026-08-08: локальный `make run` с тем же токеном забрал часть
      апдейтов у dev-контура, Telegram отвечал второму поллеру `409 Conflict`, и
      это не увидели ни логи, ни health - контур остался healthy, а автор получал
      отказ от чужого белого списка. Ошибки поллера должны попадать в лог, не
      включая `Verbose`: он дампит тексты сообщений.
- [ ] **Golden-набор из реальных обращений**: подать боту исходные голосовые и
      скриншоты, сравнить тикет с написанным руками. Единственный способ
      проверить качество интервью.
- [ ] **Один источник истины по формату ТЗ**: `plan-briefing.md` во внутренней
      базе знаний и шаблон PRD брифовщика описывают один артефакт по-разному.
      Свести до M6, иначе бот выдаст не тот формат. Вне этого репозитория.
- [ ] **Один токен - один поллер**: локальный запуск обязан отличаться токеном
      от контура, иначе он молча крадёт сообщения. Дешёвый вариант - отдельный
      тестовый бот в `.env.example` с явным предупреждением; разбираться с этим
      вместе с живостью поллера.

## Отложено осознанно

- Прод-контур. Решение владельца 2026-08-08: MVP живёт в dev-контуре целиком,
  перенос в прод обсуждается после того, как контур замкнётся. Отдельного
  milestone на прод нет и заводить его сейчас не нужно.
- Веб-интерфейс, дашборды, отчёты.
- Приоритеты, сроки, назначение исполнителей.
- Редактирование тела созданного тикета.
- Поиск и фильтры в списке тикетов.
- Автоматическая простановка меток статуса из CI: начинаем с ручной, сервис
  об этом не знает.
