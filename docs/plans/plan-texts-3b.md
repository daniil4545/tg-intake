# Сценарии проверки среза «тексты в texts.go»

Раздел 3b [plan-texts.md](plan-texts.md).

Все быстрые, без БД и сети; `texts_test.go`. Сканер - функция теста на `go/parser`:
литералы `token.STRING` с кириллицей (`unicode.Cyrillic`); `CHAR` и комментарии не смотрит
(ключи таблицы транслитерации `db.go:255` - руны, остаются на месте).

| # | Дано / когда / тогда | Источник риска | Факт проверки | Тест | Коммит |
|---|---|---|---|---|---|
| 1 | не-тестовые `.go` пакета `app` и `cmd/intake`, кроме двух файлов текстов | R9: строка осталась в хендлере | пусто; иначе в ошибке файл:строка и литерал | `TestTextsInOneFile` | `S1` |
| 2 | сканер на фикстуре: `"Привет"`, `"## Кратко\n"`, `` `сырой` ``, `"a" + "б"`; не ловит `'ё'`, `// "Привет"`, `"hello"`, `"https://x"` | тест 1 не мог бы упасть | найдено ровно 4 литерала с их строками | `TestFindCyrillicLiterals` | `S1` |
| 3 | каждое «X» без `%` в литералах `texts.go` | ссылка на кнопку не дословна (§11) | X равно значению константы `button*` или `command*`; иначе ошибка с X | `TestTextsQuoteButtons` | стиль |
| 4 | литералы `texts.go` | стиль §11 | нет «пожалуйста», целых слов база(ы,е,у), очередь(и), лог(е), эмодзи (руны U+2600-27BF и от U+1F000) | `TestTextsStyle` | стиль |
| 5 | константы подписей панели, команд и §11 | рубеж 3a | равны прежним строкам, списком в тесте | `TestPanelButtonsUnchanged` | `S1` |

Имена для §7: `TestTextsInOneFile TestFindCyrillicLiterals TestTextsQuoteButtons
TestTextsStyle TestPanelButtonsUnchanged`.
