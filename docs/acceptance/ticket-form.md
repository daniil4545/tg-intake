# Приёмка: сквозной прогон диалогов (срез 8)

Issue: galera-tasks#32, срез 8 из 8. Спека: `docs/specs/ticket-form.md` §7, §10.
Дата: 2026-09-22. Коммит: `9fb8411`. Ветка: `feature/ticket-form`.
Модели: `google/gemini-3.1-flash-lite` (медиа), `deepseek/deepseek-v4-flash-0731` (диалог) -
дефолты `config.go`. Песочница: `daniil4545/intake-sandbox`.

## Сквозной прогон

Команда - `docs/plans/plan-live-run.md` §5 (план удалён после переноса решений, история в
git). Четыре прогона `make live`, между ними правки теста - инварианты продукта не менялись.

| Прогон | Итог | Что упало и почему |
|---|---|---|
| 1 | FAIL, 2/5 | S2, S3 - тест был строже продукта (`checkTurn`); S4 - провайдер не ответил. S5 дошёл до публикации, issue #4 |
| 2 | FAIL, 0/5 | конкретный выбор модели и ответы провайдера; тест переведён с проверки выбора модели на инварианты продукта (§3 спеки) |
| 3 | FAIL, 1/5 | упавшие сценарии - несовпадение словоформы в проверке текста, не поведение продукта |
| 4 | **PASS, 3/5** | S2, S5 пропущены провайдером (7 и 8 таймаутов модели на обращение), падений нет |

Итоговая строка прогона 4 (`TestLiveRun`, `go test -tags live`):

| Сценарий | Issue | kind | gaps | events | Итог |
|---|---|---|---|---|---|
| S1 `R2` баг | #9 | bug | - | round_asked=0, summary_ready=1, published=1 | PASS |
| S2 `R2` пожелание | - | - | - | - | SKIP: провайдер, 7 таймаутов |
| S3 `R1` смесь | #10 | mixed | - | round_asked=1, answer_given=1, summary_ready=1, published=1 | PASS |
| S4 смесь (гейт B) | #11 | mixed | why | round_asked=2, answer_given=2, summary_ready=1, published=1 | PASS |
| S5 `R5` пропуск | - | - | - | - | SKIP: провайдер, 8 таймаутов |

`итог: пройдено 3/5, пропущено провайдером 2, упало 0`. `TestLiveRun` PASS
(`ok github.com/daniil4545/tg-intake/internal/app 1657.777s`).

Инварианты продукта (§3 плана: разметка одного живого экрана, ровно один ответ на нажатие,
устаревшая кнопка без побочного действия, метка `incomplete` и «Не уточнено» по факту
`rules.Unclear`, ни одна идея не выброшена, заголовки саммари без повторов) держались во всех
четырёх прогонах - падения приходились на строгость теста и провайдера, не на них.

## Замер промтов (eval)

`make -C src eval-compare`, 3 прогона по 54 обращения.

| Метрика | База | После |
|---|---|---|
| M1 | 0.116 | 0.369 |
| M2 | 1.469 | 0.901 |
| M3 | 0.832 | 0.877 |
| M4 | 2.669 | 1.717 |
| failed | 9.3% | 6.2% |

Вердикт: **pass** (порог Р-9 взят).

## Гейт B - для владельца

Issue #10 и #11 в `daniil4545/intake-sandbox` открыты, смесь бага и пожелания, читает
владелец.

Наблюдение: в теле обеих смесей одновременно есть разделы по пунктам ядра и строка
«Не уточнено» по тем же пунктам - слова автора Go сохраняет по факту упоминания (по
пробелу), а строка «Не уточнено» считается по тому, закрыт ли пункт ядра целиком; смесь
может нести слова по пункту и всё равно не закрывать его.

## Критерий приёмки

Команда прогона 4 зелёная (`plan-live-run.md` §7): S1, S3, S4 дошли до конца и держат
инварианты продукта; S2, S5 пропущены по вине провайдера, приёмке не мешают. `eval-compare`
- pass. S3 и S4 остаются открытыми для гейта B, S1 закрыт в песочнице после публикации.
