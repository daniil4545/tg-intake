# Выкат 0.2.0: паспорт релиза

Паспорт прод-релиза функции galera-tasks#32, заполнен до выката. Проверки прежнего
workflow держат этот документ и общий инструмент выката платформы; инструмента здесь нет
намеренно: репозиторий публичный.

## Состояние на 2026-09-22

- Кандидат: PR #43, `feature/ticket-form` в `prod`, код на `6aafad2`, CHANGELOG на `0f4696c`.
  Коммит тега появится мерджем.
- Чужая работа в репозитории: worktrees срезов в `.claude/worktrees/` (`slice-live-screen`,
  `slice-skip`, `slice-nav`, `slice-texts`, `slice-live`, `eval-base-run`, агентский) - их
  содержимое уже в ветке функции, ветки не запушены; worktree `fix/prod-compose-no-proxy`
  (0.1.14) слит в `prod` PR #42. Старая `feature/tickets-paging` к релизу не относится.
- Версия `v0.2.0`, шаг minor: новое пользовательское поведение, восемь срезов одной функции.
- Приёмка среза: `docs/acceptance/ticket-form.md` - ждёт среза 8.
- Прод сейчас: `2a99dfb` (образ 0.1.13; `v0.1.14` - тот же образ, только compose), схема БД
  `0010`.
- Что едет пользователю: меньше вопросов (только ядро, до двух раундов), тикет свободными
  разделами и тип «смесь», кнопка «Отправить как есть», один живой экран и единые тексты.

## Развилки этого релиза

| Развилка | Значение | Что меняет |
| --- | --- | --- |
| Миграции | `0011` (DDL и бэкфилл), `0012` (DDL) | `app` останавливается в Coolify до `migrate`, окно 30 минут |
| Бэкфилл | `0011` правит обращения в `interview`, `summary`, `publishing` | 22.09 таких 0 из 66 (51 `published`, 15 `cancelled`); пересчитать перед выкатом |
| Числа наружу | нет | отчётов и метрик для людей релиз не меняет |
| Compose | этим PR нет; в `prod` compose 0.1.14 | `sync-compose` снимет пин `api.telegram.org` и сделает прокси OpenRouter необязательным: выкат только на новый сервер, см. «Остаток риска» |
| Новые переменные | нет | `INTERVIEW_ROUNDS` в контуре заведена; значение сверить до выката: пусто или 2 - новый дефолт 2 действует, 3 - нет |
| Кандидат | PR #43 | мердж на шаге 5; перед ним конфликт CHANGELOG с `prod` |
| Пользовательские изменения | есть | анонс - секция `### Анонс` раздела `0.2.0` |
| Откат | ограничен | см. «Откат и граница» |

## Ворота до выката

| Ворота | Состояние |
| --- | --- |
| G0 разрешение | ждёт команды владельца с проектом, контуром и версией |
| G1 проверка до прода | dev-контура нет; замещает живой прогон среза 8 в песочнице (`tg_send`/`tg_read`, issue в `intake-sandbox`) - ждёт среза 8 |
| G2 исходный код | 22.09 на `6aafad2`, чистая база: `make ci-check` зелёный (миграции 0001-0012 вверх, тесты, `vet`, `lint` 0 issues, `build`), `make test RUN='.'` зелёный; цели `deploy-check` нет. Повторить на коммите мерджа |
| G3 образ | не собран: `sha` появится мерджем |
| G4 данные | миграции `0011`, `0012`; шага backup в стеке нет, база под ночным дампом хоста |
| G5 Compose | PR не меняет; `sync-compose` первым внутри `release` увезёт compose 0.1.14 |
| G6 активная работа | 22.09: `cases` - 0 обращений в работе, последняя активность 21.09 13:32; `job-errors` - два старых `failed` (08.08, 17.08), новых нет. Перепроверить перед выкатом |
| G7 deploy | тег `v0.2.0` на коммите мерджа в `prod` |

Завершение среза по `pre-release.md` - 22.09, агент релизной подготовки; открытые пункты -
«Чек-лист завершения».

## Чек-лист завершения

| Пункт | Состояние |
| --- | --- |
| 1. Ветки среза | сделано: ветки срезов влиты в `feature/ticket-form` (`git cherry` пуст, `slice-texts` сквошен в `8f15bde`) |
| 1. Worktrees | названы выше; релиз идёт из каталога репозитория на `prod` |
| 1. Заглушки, TODO, отладка | сделано: в добавленных строках `src/` нет `TODO`, `FIXME`, отладочного вывода |
| 1. Временные скрипты | сделано: `eval-export` - часть канона `safe-ssh.sh`, набор и результаты вне git |
| 1. Флаги прогона | открыто: значение `INTERVIEW_ROUNDS` в контуре |
| 2. След прогонов | сделано для срезов 1-7: прогон eval на локальной базе, в песочнице issue нет; уборка после среза 8 - ждёт среза 8 |
| 3. Сверка со спекой | тесты `R1`-`R6`, `R8`, `R9` в `ci-check`; порог Р-9 взят (`R7`); диалоги `R1`, `R2`, `R5` и тикет по смеси - ждёт среза 8 |
| 4. CHANGELOG | сделано: раздел `0.2.0` с анонсом, миграциями и переменными |
| 4. `docs/state.md` | открыто: описывает срез 1, обновит `finish` главного окна |
| 4. Каноны | каноны правлены срезами (`prd`, `architecture`, `contracts`, `llm`, `AGENTS.md`); отдельный `/doc-audit` не проводился |
| 4. Подспеки `docs/plans/` | ждёт среза 8: удаляются после переноса решений; `plan-prompts-eval.md` 13,5 КБ больше лимита 12 КБ |
| 4. Лимиты | `CHANGELOG.md` 31 КБ при лимите 8 КБ - превышение прежнее, релиз не блокирует, свёртка старых версий отдельной уборкой |
| 5. Backlog и issue | #32 в `backlog.md`; `status:in-progress`, `status:dev` после среза 8 |
| 6. Паспорт среза и гейт B | ждёт среза 8 |
| 7. Карточка контура | неприменимо: адрес, порт, состав и процедуру релиз не меняет; карточку меняет переезд |

## Окно выката

22.09 около 11:40 МСК: `cases` - обращений в `interview`, `summary`, `publishing` нет,
последнее обновление 21.09 13:32; `job-errors` - только старые `failed` от 08.08 и 17.08.
Рассылок по расписанию нет; слежение за тикетами раз в пять минут остановка лишь откладывает. Окно свободно на момент проверки; при DDL
порог 30 минут, время по умолчанию - утро до 09:00 МСК. Перед выкатом повторить обе команды.

## Порядок

```sh
# 1. Мердж: конфликт CHANGELOG - версия ветки уже несёт разделы 0.2.0 и 0.1.14
gh pr merge 43 --merge && git checkout prod && git pull --ff-only

# 2. Тег на коммите мерджа
git tag -a v0.2.0 -F <файл с телом тега> && git push origin v0.2.0

# 3. Сверить INTERVIEW_ROUNDS в Coolify (новых переменных нет)

# 4. Образ и проверка в реестре
CONFIRM=yes coolify-deploy.sh intake build <sha>
docker manifest inspect ghcr.io/daniil4545/tg-intake:sha-<sha>

# 5. Остановка app в Coolify до migrate (DDL 0011, 0012)

# 6. Выкат: Compose, APP_IMAGE, deploy
CONFIRM=yes coolify-deploy.sh intake release <sha>
```

## G8. Приёмка

1. `app_revision` из `deploy/safe-ssh.sh health` равен `<sha>`.
2. `app_health=healthy`, `postgres_health=healthy`, `migrate_state=exited:0`, перезапусков нет.
3. `app-logs 200` без ошибок конфигурации, Telegram, OpenRouter, GitHub; в логах `migrate`
   нет `lock_timeout`, схема 0012.
4. Stop signals по `runbooks/prod-health-check.md` базы отсутствуют.
5. Функциональная проверка: живое обращение-смесь по проекту qualifier - вопросов не
   больше двух раундов, в раунде есть «Отправить как есть», у прошлого экрана сняты
   кнопки, issue опубликован свободными разделами; `case <id>` - статус `published`,
   работы в `done`. Тестовый тикет после проверки закрывается.

## Разбор отказов

Общий - `release-record.md` скилла `release`. Свой: `getMe` в таймаут после выката -
compose 0.1.14 без пина DC на прежнем сервере, откат или выкат на новый сервер.

## G9. Анонс

Секция `### Анонс` раздела `0.2.0` в `CHANGELOG.md` заполнена, четыре пункта, тикет #32.
Цифры M1 и M4 в первом пункте - из прогона eval, не с прода. Публикация только после G8.

## Откат и граница

Предыдущая ревизия контура: `2a99dfbc00d6bfa33e9179be019896b5adf5b90b` (образ 0.1.13).

- Откат сразу: контур не поднялся или функциональная проверка провалилась.
- Правка вперёд patch: сервис живой, потерь нет, ошибочных касаний наружу не было.

Механика: возврат на образ 0.1.13 через `build`/`release`, `goose down` не запускается,
Compose остаётся текущим. Что сломается при откате: обращения, начатые на 0.2.0 с типом
`mixed` или ключами ядра, прошлый бинарь не знает - их доводит автор заново; колонки
`0012` старому коду не мешают, опубликованные тикеты не затронуты.

## Остаток риска

- Срез 8 не пройден: живые диалоги `R1`, `R2`, `R5`, оценка тикета по смеси владельцем
  (гейт B), паспорт `ticket-form.md`.
- Сервер выката: compose 0.1.14 из `prod` рассчитан на новый сервер. На прежнем без пина
  `api.telegram.org` Telegram не отвечает (наблюдение 08.08). Выкат 0.2.0 - после переезда
  или в его окне; иначе нужна отдельная правка compose.
- `INTERVIEW_ROUNDS` в контуре: при значении 3 прод идёт с тремя раундами, цифры прогона
  (измерены при 2) к нему не относятся.
- Промт проверен на прогоне прошлых обращений, а не на живых авторах.
