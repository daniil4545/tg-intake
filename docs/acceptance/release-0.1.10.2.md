# Выкат 0.1.10.2: чеклист ручного релиза

Ручной путь отменяет исполнителя шагов, не ворота. Пять автоматических проверок
workflow (исполнитель-владелец, ref-тег, формат версии, предок ветки, явное
подтверждение) держит теперь этот чеклист - это главное, что теряется.

Канон шагов - `runbooks/coolify-service-api.md` базы знаний, раздел «Выкат
руками»; ворота - `runbooks/deploy-service.md`.

## Состояние на 2026-08-17

- Кандидат: PR #33, ветка `feature/lookup-read-check`, шесть коммитов.
- Приёмка среза: [lookup-read-check.md](lookup-read-check.md).
- Право `Contents: read` у `GITHUB_TOKEN` расширено владельцем 17.08.2026,
  живым прогоном не подтверждено - это и проверяет G8.

## Ворота до выката

| Ворота | Состояние |
| --- | --- |
| G0 разрешение | ждёт команды владельца |
| G1 проверка до прода | `N/A`: отдельного dev-контура нет, замещающее доказательство - `make ci-check` и тесты среза |
| G2 исходный код | `make ci-check` зелёный на кандидате; CI на PR не запускался, минуты Actions исчерпаны |
| G3 образ | не собран: sha кандидата меняется мерджем в `prod` |
| G4 данные | миграций нет; шага backup у контура нет, база под ночным дампом хоста |
| G5 Compose | `deploy/prod/docker-compose.coolify.yml` не менялся срезом |
| G6 активная работа | проверить `job-errors` и `case-trace` перед выкатом: в очереди не должно быть активных обращений |
| G7 deploy | тег `v0.1.10.2` из `prod` после мерджа |

## Порядок

Все команды из корня репозитория. `<sha>` - коммит в `prod` после мерджа PR.

Шаги выката выполняет общий инструмент платформы `infra/scripts/coolify-deploy.sh`
из внутренней базы знаний - один на все контуры; здесь его нет намеренно,
репозиторий публичный.

```sh
# 1. Мердж и тег (владелец)
gh pr merge 33 --merge
git checkout prod && git pull --ff-only
git tag -a v0.1.10.2 -F <файл с телом тега>

# 2. Проверки на кандидате
make -C src ci-check

# 3. Образ с рабочей машины
coolify-deploy.sh intake build <sha>

# 4. Выкат: Compose, APP_IMAGE, деплой - строго в этом порядке
CONFIRM=yes coolify-deploy.sh intake release <sha>

# 5. Приёмка
deploy/safe-ssh.sh health
deploy/safe-ssh.sh app-logs 100
```

Инструмент сам не пустит прод дальше без `CONFIRM=yes`, чистого рабочего дерева
и тега `v*` на коммите - это три из пяти проверок, потерянных вместе с workflow.
Ответ Coolify 200 означает «запрос принят», не «версия выкачена».

## G8. Приёмка

1. `app_revision` из `health` равен `<sha>`. Старый контейнер остаётся healthy и
   при полностью несостоявшемся выкате - это первое, что сверяется.
2. `postgres_health=healthy`, перезапусков нет.
3. В логе старта на каждый активный проект пара `github_write_ok` и
   `github_read_ok`, ни одного `github_read_denied`.
4. Живой вопрос в режиме «Спросить» по проекту qualifier: в логе `lookup_start`,
   `lookup_pick` с числом файлов, `lookup_done`; в чате ответ со ссылкой на
   md-файл. `deploy/safe-ssh.sh case <id>` - работа `lookup` в `done`.

Пункт 4 обязателен: он и есть проверка того, ради чего релиз делался. Пункты
1-3 без него доказывают только то, что контейнер поднялся.

## G9. Анонс

Секция `### Анонс` версионного раздела `0.1.10.2` в `CHANGELOG.md` заполнена.
Публикуется скиллом `announce` только после G8: пост заказчику не отзывается.

## Откат

Schema-compatible: миграций релиз не несёт, `goose down` не запускается.
Возврат `APP_IMAGE` на предыдущий `sha-` и повторный выкат тем же инструментом.
Предыдущая ревизия контура - `a2c8e6b663a8218185742664e8bdb075e5ccac68`.
