# Состояние проекта

Задача: нет в работе | Шаг: - | Блокер: нет | От владельца: ничего.

Обновлено: 2026-09-22. В проде: 0.2.0 (2026-09-22, ревизия `7273a8d`, тег `v0.2.0`), прод на
старом VPS, dev-контура нет. Спека последней функции - [specs/ticket-form.md](specs/ticket-form.md),
порядок работ - [backlog.md](backlog.md), решения - [architecture.md](architecture.md).

## Сделано

- 21.09, глобальная спека #32 (фаза Spec), `spec-review` два прохода pass with fixes.
- Срезы 1-8 выполнены, слиты в `feature/ticket-form` (`git cherry` пуст). Срез 8 - сквозной
  прогон в песочнице `daniil4545/intake-sandbox`: 4 прогона, финальный PASS (3/5, S2 и S5
  пропущены провайдером), `eval-compare` pass. Паспорт -
  [acceptance/ticket-form.md](acceptance/ticket-form.md).
- 22.09, релиз 0.2.0 (galera-tasks#32 закрыт): ревью PR #43 - must-fix (тело issue из
  необезличенного протокола, Р-18) и should-fix (дубль issue на новой работе публикации,
  правка после пропуска, ПДн в ошибках формата, `#57` как заголовок) исправлены с тестами;
  пин `api.telegram.org` возвращён в compose; `INTERVIEW_ROUNDS=2` в контуре; анонс в канале.
  Проверка: `make ci-check` и `make test RUN='.'` зелёные, Coolify `running:healthy`, бот
  отвечает в Telegram.

## В работе

Ничего.

## Дальше

1. Приёмка 0.2.0 не добрана (SSH к VPS висел на баннере 22.09): `deploy/safe-ssh.sh health`:
   `app_revision` = `7273a8d`, `migrate_state=exited:0`, `app-logs 200` без ошибок; первое
   живое обращение - кнопка «Отправить как есть» в раунде.
2. В окне переезда на узел A (#9) снять пин `api.telegram.org` в compose.

Следом по бэклогу: #2 (тикеты в galera-tasks), #31 (ответ на комментарий из бота).

## Открытое

- Локальные ветки срезов #32 (`slice-*`, `eval-base-run`, `worktree-agent-acf502c90aeea5c41`)
  не удалены: `git branch -D` в deny, релиз вышел - удаляет владелец.
- 0.2.0 выкачен без остановки `app` до `migrate` (инструмента нет): ответ автора в секунды
  миграции мог уйти старыми ключами (находка 4 ревью).
- Nice-to-have ревью: значения `Filled` в теле issue не проверяются на строку-заголовок.
- Провайдер модели медленнее `llmTimeout` 60 с на заметной доле ходов - в прогонах среза 8
  по 7 и 8 таймаутов на обращение (`docs/acceptance/ticket-form.md`).
