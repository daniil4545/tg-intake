# Состояние проекта

Обновляется при каждом коммите. Дата последнего обновления: 2026-08-13.

## Сделано

- Этап 1 (MVP) закрыт релизом 0.1.0 (2026-08-09): M0-M4 плюс кнопочное
  управление. Обращение из Telegram доходит до тикета GitHub и его статуса без
  участия владельца; живые прогоны - issues 8 и 9 в `tg-intake`, 28 и 29 в
  `qualifier`. Спеки в [plans/](plans/), паспорта в [acceptance/](acceptance/).
- Предрелизный аудит 2026-08-09 четырьмя независимыми ревью (код, архитектура,
  сценарии, чистка): блокеров нет, находки закрыты правками либо занесены в
  [backlog.md](backlog.md); документация сверена с кодом и ужата в лимиты.
- Схема репозитория с 0.1.0: единственная ветка `prod`, деплой контура только
  по тегу `v*` workflow `Deploy`, автодеплой по push убран. Единственный контур
  считается прод.
- 0.1.1 (2026-08-10): показанное саммари попадает в историю диалога репликой
  бота, снимок текста лежит в событии `summary_ready`. Спека -
  [plans/plan-dialog-memory.md](plans/plan-dialog-memory.md).
- 0.1.2 (2026-08-12): подпись под медиа доходит до модели отдельной строкой и
  ссылки уходят разделом в тело issue; публикация и отмена тикета кладут шапку
  в чат уведомлений владельца (`ALERT_CHAT_ID`, `ALERT_BOT_TOKEN`).
- 0.1.4 (2026-08-12): интервью получило глубину, порядок и прицеп -
  контракт с границей готовности и исключениями
  ([plans/plan-interview-depth.md](plans/plan-interview-depth.md)), порядок
  вопросов и уточнения внутри вопроса
  ([plans/plan-interview-tree.md](plans/plan-interview-tree.md)), запрет
  обобщающей догадки, ключи пробелов в логах.
- 0.1.5 (2026-08-13): убрана перекодировка голосового через ffmpeg - режима
  `AUDIO_CONVERT` больше нет, образ его никогда и не нёс; документация сверена
  с кодом аудитом и введена в лимиты.

## В работе

Релиз 0.1.5 в контуре. Ждёт живого прогона: спрашивает ли бот про признак
готовности и про исполнителя ручных шагов, просит ли адрес объекта внутри того же
вопроса, перечисляет ли предположение названные поля поимённо, не выросли ли число
раундов и доля тикетов с меткой `incomplete`. С прошлых релизов наблюдения не
закрыты: правка саммари после 0.1.1 (меняет ли она только названный раздел),
подпись со ссылкой в теле тикета и время шага интервью после поднятого бюджета.

Разбор жалобы на issue 35 в `qualifier` (2026-08-12): бот получил шесть строк
разбора скриншота вместо рассказа автора - подпись под медиа выпадала из протокола
до 0.1.2. Сам дефект закрыт, тело issue 35 восстановлено вручную из базы; тикеты
до 12.08 с фото содержат меньше сказанного - в 30, 31, 33 и 37 потеряны ссылки на
карточки сделок, правку владелец не заказывал.

Аудит документации 2026-08-13 сверил AGENTS, README, architecture, prd, contracts
и backlog с кодом; заодно убрана нерабочая перекодировка голосового через ffmpeg
(образ `distroless/static` его не содержал). Выкачено релизом 0.1.5.

## Дальше

Этап 2 по [backlog.md](backlog.md): первым M6 (брифинг нового проекта), следом
M7 (вопросы по документации), M8 (уведомления о статусе), M9 (комментарии).
Вход M6 - живой прогон промта брифовщика с заказчиком вне бота.

Требует наблюдения, а не работы: порог готовности зависит от формы обращения;
диалог перегружен шагами; уведомление, исчерпавшее повторы, молчит - список в
[backlog.md](backlog.md).

Рассмотреть позже, решением владельца 2026-08-12 сейчас не берём: привязка
документации проекта к контексту модели. Направление подсказано заказчиком,
источник - скилл `user-spec-planning` в `github.com/pavel-molyanov/molyanov-ai-dev`.
Там знание о проекте это четыре файла базы знаний плюс свежий скан кода перед
уточняющими вопросами, и та же база потом судит полноту результата. У нас на том
же месте одно поле `projects.context`: абзац, сочинённый моделью из README при
`/project` и больше не обновляемый. Переносимы три слоя: структура вместо
абзаца (назначение, словарь пользователей, связанные системы, границы «чего
сервис не делает»), обновляемость карточки и второй слой из прошлых тикетов
проекта - наш аналог их скана кода. Ограничение реализации: карточка идёт в
кэшируемый префикс промта, список тикетов волатилен и обязан лежать позади него.
Прямой перенос их четырёх файлов не годится: они писаны для разработчика, а
стек, каталоги и переменные окружения уводят разговор с сотрудником в технику -
это же запрещает `prompts/project.md`.

## Решения

- **2026-08-12. Бюджет попытки к модели считается по измеренной длительности
  шага, а не круглым числом.** Шаг интервью идёт около 50 секунд при прежнем
  пределе 60: разброс отменял готовую генерацию, автор ждал три прохода, токены
  списывались за каждый. Попытке две минуты, повтору порог в минуту; синхронный
  `/project` с бюджетом 20 секунд повторов лишился осознанно. Метрика `ms`
  теперь про одну попытку - прежняя сумма прятала то, ради чего её читают.
- **2026-08-12. Уведомление владельцу шлёт сам сервис, адресат - чат в
  конфиге.** Владелец не видел, какие тикеты заводятся и в какие проекты.
  Ролей в сервисе по-прежнему нет: получатель это `chat_id`, круг читателей
  решается составом чата. Токен берётся из настроек Telegram в Coolify, чтобы
  тикеты шли одной лентой с алертами контура; пустой токен отдаёт отправку боту
  сервиса. Отменяет заготовку про workflow на `issues.opened`: тот же результат
  без секретов в каждом репозитории, но и только по тикетам, заведённым ботом.
- **2026-08-10. Подпись под медиа - слова автора, ссылки в тикет кладёт Go.**
  Решение владельца по разбору issue 31 в `qualifier`: ссылка на карточку и
  требование «менять статус на связаться позже» пришли подписями к скриншоту и
  голосовому и потерялись молча, тикет вышел без пометки о неполноте.
  Пересылается содержимое элемента, подпись автор набирает сам, поэтому
  подтверждения она не требует. Адреса собираются кодом отдельным разделом:
  модель законно сокращает текст, а обрезанный URL ведёт в никуда.
- **2026-08-10. Модель видит переписку с автором целиком, включая показанные
  саммари.** Решение владельца по разбору живого прогона (issue 30 в
  `qualifier`): выборка истории брала только вопросы и ответы, поэтому правку
  саммари модель получала без текста, который правят. Снимок текста лежит в
  событии `summary_ready`: колонка `cases.summary` держит только последнюю
  версию, а цепочка правок нужна целиком. Внутреннее состояние контракта
  модели по-прежнему не показывается - оно производное от диалога, и прошлые
  формулировки рядом с сырьём толкают модель пересказывать себя.
- **2026-08-10. Обращение с плотным сырьём проходит без вопросов - это норма.**
  Тикет 30 закрыл контракт `bug` из двух скриншотов на первом ходу. Спрашивать
  модель может только про пункт, объявленный пробелом, поэтому три крупных
  обязательных пункта закрываются одним разбором. Наблюдение остаётся в
  [backlog.md](backlog.md), планка промта не поднимается до накопления случаев.
- **2026-08-09. Одна ветка `prod`, деплой только по тегу.** Решение владельца
  при закрытии MVP: пользователи - владелец и менеджеры, внешних нет, поэтому
  отдельный dev-контур и релизная пара веток избыточны. Рабочий цикл:
  `feature/<slug>` -> PR в `prod` -> тег `v*` -> выкат. Понадобится песочница -
  dev-контур заводится заново по runbook.
- **2026-08-09. MVP закрыт решением владельца, гейт B пройден.** Продукт
  выполняет начальную цель - даёт создавать тикеты к сервисам.
- **2026-08-09. Саммари достраивается из контракта, а не требует пересказа.**
  Живой прогон: модель пять раз подряд вернула ответ без разделов, валидация
  Go отклоняла его, работа крутилась в повторах, автор ждал молча четыре
  минуты. Содержание пунктов уже собрано интервью, поэтому недостающий раздел
  берётся из контракта; за моделью остаются заголовок и формулировки, и её
  молчание больше не останавливает тикет.
- **2026-08-09. Пункт контракта становится вопросом не больше двух раз.**
  Живой прогон: раунд задаёт несколько вопросов, человек отвечает одной
  репликой на один из них, и модель законно спрашивает остальные снова -
  автор видит дословный повтор и читает его как поломку. Счёт по журналу
  раундов в Go, а не надежда на промт: исчерпанный пункт уходит в тикет
  пробелом с пометкой о неполноте.
- **2026-08-09. Управление кнопками, свободный текст - только содержимое
  обращения.** Решение владельца после первого живого прогона: пользователь
  пишет боту беспорядочно, и надеяться на «по плану» нельзя. Сообщение вне
  обращения не сохраняется, бот отвечает начальным экраном; постоянная
  reply-панель («Меню | Сброс», в сборе «Готово | Сброс»); глобальный «Сброс»
  вместо «Отмены», «Начать заново» и `/cancel` - с подтверждением, когда есть
  материал. Отменяет решение 2026-08-08 «сырьё принимается до выбора проекта».
- **2026-08-09. Медиа-инвариант сохранён, дебаг - через safe-ssh.** Владелец
  хотел хранить всё присланное для разбора некорректных тикетов; выяснено, что
  тексты уже хранятся в БД, а файлы перекачиваются по `file_id`. Вместо
  локального хранения - read-only команды `cases`, `case <id>`, `job-errors`,
  которым разрешено выводить содержимое обращения в терминал.
- **2026-08-09. Проект заводится командой `/project <ссылка>` в боте.**
  Отменяет вчерашнее решение о переменной окружения: JSON руками в панели
  контура плюс рестарт - не тот способ, которым будут пользоваться. Довод
  «команда потребовала бы роли владельца» снят: заводить может любой из белого
  списка, потому что fine-grained токен всё равно ограничен своими
  репозиториями, а проект вне их не заведётся. `PROJECTS` остаётся сидом для
  чистой базы и существующие строки больше не перезаписывает - иначе рестарт
  молча откатывал бы правки. Инвариант вернулся к исходному: добавление проекта
  не требует ни выката, ни рестарта.
- **2026-08-08. Список проектов задавался переменной окружения контура.**
  Отменено решением 2026-08-09 выше, переменная осталась сидом. Причина выбора
  была в том, что репозиторий публичный: миграция с именами внутренних сервисов
  отпадает, прямой `psql` отпадает - диагностика через `safe-ssh.sh` read-only.
- **2026-08-08. Просмотр ходит в GitHub синхронно, но отдельным клиентом.**
  Бот обрабатывает апдейты последовательно, и один отказ с рабочим бюджетом
  заморозил бы его для всех авторов почти на полминуты. У просмотра свой
  клиент: пять секунд, без повторов, отказ сразу даёт «статус недоступен».
- **2026-08-08. Работа обращения существует в единственном экземпляре, новая
  заменяет прежнюю.** Одно правило вместо трёх частных: повторное «Готово»,
  дописанный ответ и повторное «Публикую» после исчерпанных повторов ведут себя
  одинаково. Ход при этом несёт версию разговора и проверяет её при записи -
  иначе результат, начатый до ответа автора, ложится поверх свежего. Дубль
  наружу закрывает не очередь, а состояние: номер issue и маркер в теле тикета.
- **2026-08-08. Прямой путь к OpenRouter с хоста закрыт.** Адреса прокси
  приходят переменными и задаются каждому клиенту отдельно: GitHub доступен
  напрямую, и уводить его в туннель незачем. Свойство хоста, а не сервиса -
  записано в карточку инфраструктуры базы знаний.
- **2026-08-08. Контракт готовности хранится в JSON, а не в YAML.** Разбор есть
  в стандартной библиотеке, пятая зависимость ради одного файла правил не
  окупается. Правка состава пунктов по-прежнему не требует ни миграции, ни
  правки кода.
- **2026-08-08. История интервью строится из `case_events`.** Диалог по природе
  append-only, журнал уже пишется в тех же транзакциях, что и смена статуса.
  Отдельная колонка дала бы вторую копию тех же данных, отдельная таблица -
  третью сущность ради шести записей. Для брифинга модель не меняется: событий
  просто больше.
- **2026-08-08. Кнопка «Поправить» состояние не меняет.** Правкой становится
  следующее сообщение автора: ответ при показанном саммари возвращает обращение
  в интервью сам. Отдельное состояние «ждём замечание» дало бы восьмой статус
  ради одной подсказки, а голосовая правка работает тем же путём.
- **2026-08-08. Сервис определён как единая точка входа бизнеса в разработку.**
  Решение владельца. Прежнее определение - приёмник фидбека по работающим
  сервисам - описывало первую функцию, а не замысел. Четыре функции и их
  порядок в разделе 2 [prd.md](prd.md). Объём первой версии не меняется.
- **2026-08-08. После M2 идёт M4, брифинг - следом.** M4 замыкает контур первой
  функции: без статусов автор возвращается спрашивать в личку. Срочность
  брифинга снята тем, что промт брифовщика работает в чате Claude уже сейчас,
  без бота, и метрику «владелец продукта вне перевода требований» закрывает
  промт, а не канал доставки.
- **2026-08-08. Под второй режим впрок не закладываем ничего.** Список мест -
  раздел 13 [architecture.md](architecture.md). Три правки схемы дешёвые,
  четвёртая нет: одно активное обращение на автора держит маршрутизацию каждого
  входящего сообщения, и её переделка это решение продукта, а не миграция.
  Заложены только три дисциплины в коде, которые не стоят ни строчки:
  нормализация не знает об исходе, обработчики очереди по типу работы, контракт
  готовности данными. Первые две уже соблюдены в M1, проверено по коду.
- **2026-08-08. Сырьё принимается до выбора проекта, пересылка равна своему
  сообщению.** Первая половина отменена решением 2026-08-09 выше: сбор
  начинается только кнопкой. Вторая осталась: пересылка равна своему сообщению,
  имя исходного отправителя не сохраняется - авторству оно не нужно, а
  перепиской с клиентом стало бы персональными данными.
- **2026-08-08. Срок стал плавающим, объём первой версии жёстким.** Решение
  владельца после оценки: M1 плюс M2 это два полных среза, а за понедельником
  нет внешнего события. Сырой поток к дате вернул бы людей в личку.
- **2026-08-08. Апдейты Telegram обрабатываются последовательно.** telebot по
  умолчанию запускает хендлеры параллельно; на пачке пересланных сообщений это
  даёт случайный порядок сырья, двойное создание обращения и повторный вопрос о
  проекте. Одна строка настройки дешевле любой блокировки при таком потоке.
- **2026-08-08. Хранилище тикетов - GitHub Issues в репозитории проекта.**
  Альтернативы (отдельный репозиторий-агрегатор, своё хранилище в Postgres)
  отклонены: тикет должен лежать рядом с кодом, и это не требует писать
  просмотр и хранение самим.
- **2026-08-08. Статус тикета - метка на issue, единственный источник истины.**
  Убирает из проекта разбор коммитов и публичный вебхук-эндпоинт: сервис
  опрашивает GitHub и уведомляет об изменении метки.
- **2026-08-08. Медиа не хранится после обработки.** Решение владельца.
  Следствие: подтверждение саммари автором обязательно, а не желательно.
- **2026-08-08. Нормализация медиа отдельными шагами до диалога.** Экономия
  возникает не на первом проходе, а на том, что изображение не переотправляется
  в каждом ходе диалога, и на дешёвой модели для механического извлечения.
- **2026-08-08. Доступ по белому списку Telegram ID.** Отменяет прежнее решение
  об открытом доступе. Открытый бот означает запись в приватные репозитории для
  любого, кто нашёл его в поиске. Список в конфиге стоит два десятка строк кода
  и снимает из первой версии триаж, лимиты и защиту от мусора целиком.
- **2026-08-08. Авторство фиксируется телом issue и меткой `author:<slug>`.**
  GitHub не даёт создать тикет от чужого имени: автором становится владелец
  токена. Настоящее авторство требовало бы GitHub-аккаунта у каждого сотрудника
  и разовой OAuth-авторизации через публичный callback, то есть лишний день
  работы и порог входа там, где сейчас человек просто пишет в Telegram.
- **2026-08-08. Скриншоты разбираются после текста и голоса, с протоколом сырья
  в контексте.** Без контекста жалобы экстрактор не знает, что на карточке
  важно, и вычитывает всё подряд. Инвариант «изображение входит в контекст один
  раз» сохраняется: в диалог попадает только текст.
- **2026-08-08. Интервью раундами по 2-3 вопроса вместо одного за раз.** Пять
  последовательных вопросов - это пять кругов ожидания ответа; пачка из пяти
  теряет часть ответов. Каждый вопрос идёт с предполагаемым ответом, кнопка
  «Всё так» принимает раунд целиком.
- **2026-08-08. Тип «вопрос» не отдельный исход, а тикет с меткой `question`.**
  Ответ из документации требует источника доков и способа проверить ответ;
  это перспектива бота-консультанта, а не первая версия.
- **2026-08-08. Язык сервиса - Go.** Цепочка сборки и деплоя (GHCR image,
  distroless, goose в образе, Coolify Compose) отлажена на соседних сервисах
  платформы; своего ML-кода нет, только HTTP-вызовы, поэтому Python не даёт
  выигрыша и требует параллельной цепочки.
- **2026-08-08. Модели по шагам, обе runtime-настройкой.**
  `google/gemini-3.1-flash-lite` на голос и скриншоты (самая дешёвая с аудио на
  входе и структурированным выводом), `deepseek/deepseek-v4-flash-0731` на
  интервью и саммари. При единицах обращений в день разница между дешёвой и
  сильной моделью меньше доллара в день, поэтому выбор решает golden-набор.
- **2026-08-08. Очередь и outbox - одна таблица `jobs`.** Отклонение от
  `platform.md`: механизм захвата и повторов у них совпадает, отличие только в
  идемпотентности, которую даёт поле `key`. Вынесено на гейт A.
