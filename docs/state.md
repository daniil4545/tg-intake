# Состояние проекта

Задача: galera-tasks#32, свободная форма тикета и единый стиль кнопок | Шаг: срез 1,
подспека прошла ревью, дальше код | Блокер: нет | От владельца: два
ключа среза 8 (тестовый бот, GitHub песочницы).

Обновлено: 2026-09-21. В проде: 0.1.13 (2026-08-20, ревизия `2a99dfb`), dev-контура нет.
Ветка функции: `feature/ticket-form`. Спека -
[specs/ticket-form.md](specs/ticket-form.md), приложение -
[specs/ticket-form-appendix.md](specs/ticket-form-appendix.md). Порядок работ -
[backlog.md](backlog.md), решения - [architecture.md](architecture.md).

## Сделано

- 21.09, глобальная спека #32 (фаза Spec). Разведка тремя субагентами: поток контракта
  через код и БД, каталог экранов и сообщений, канон и 49 тикетов бота (26 bug, 22 feature,
  1 question; «Не разобрано» у 18; уточнять у автора пришлось 1 раз). Решения владельца
  21.09: тип - подсказка со «смесью», минимальное ядро, свободные разделы, «Отправить как
  есть», прогон до и после, единый стандарт экранов и стиль текстов. `spec-review`: два
  прохода pass with fixes, находки закрыты в тексте; третий по канону не проводится.
- 21.09, срез 1: подспека [plans/plan-eval-base.md](plans/plan-eval-base.md), `spec-review`
  2 must-fix и 7 should-fix закрыты текстом. Проверено на проде: payload `round_asked` несёт
  вопросы, `answer_given` - `round` и `text`; 58 из 66 обращений прошли нормализацию.
  Создана приватная песочница `daniil4545/intake-sandbox`.
- Тело #32 переписано под рамку, тикет в `status:in-progress`, комментарий с планом.

## В работе

#32, срез 1 из 8: подспека готова, код не начат. Подспека 9 КБ, больше порога 4 КБ, поэтому
исполняется новой сессией. Ключ OpenRouter - в `src/.env`, Makefile его
подхватывает. Прод-чтение: `INTAKE_SSH_HOST=root@5.42.118.105
deploy/safe-ssh.sh <действие>` одиночной командой, в конвейере его режет классификатор.

## Дальше

1. Срез 1 - по подспеке, ключ в `src/.env`. Срез 2 (ядро и
   свободная форма, миграция `0011`) ключа не ждёт и может идти первым.
2. Срезы 3-7 по зависимостям §13; срез 4 (живой экран, `0012`) - после 2.
3. Срез 8, живой прогон - нужны токен тестового бота
   (`galera/intake-dev/bot TELEGRAM_BOT_TOKEN`) и токен GitHub на песочницу
   (`galera/intake-dev/github GITHUB_TOKEN`); песочница `daniil4545/intake-sandbox` есть.

Следом по бэклогу: #2 (тикеты в galera-tasks), #31 (ответ на комментарий из бота).

## Открытое

- Снимаются ли inline-кнопки у сообщения старше 48 часов - замер в срезе 4 до кода.
- Ждёт живого прогона с прошлых релизов: сверка черновика (0.1.13), режим «Спросить»
  (0.1.10, право `Contents: read` живьём не подтверждено), новости по тикету (0.1.11-0.1.12).
  Сценарии интервью и саммари из этого списка перекрывает срез 8.
- `docs/plans/` держит пять спек выкаченных срезов; #17 закрыт, их судьба - на следующей
  уборке документации.
- Спека функции в `feature/ticket-form`, в `prod` её нет до мерджа ветки.
